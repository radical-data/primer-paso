# Certificate template

Currently, we use this template as a flat PDF and draws a first-pass overlay onto it.

In future, we may add AcroForm fields to a derivative template and fill named fields instead of drawing text by coordinate.
