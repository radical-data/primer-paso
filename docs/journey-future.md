# Journey notes for deferred work

This file is intentionally limited to journey work that is not yet the canonical implemented flow.

The implemented journey lives in:

- `apps/public/src/lib/journey/config.ts`
- `apps/public/src/routes/**`

## Deferred items

- stronger save-and-resume beyond same-browser continuity
- support-oriented summary and explicit sharing consent where handoff is implemented
- fuller localisation coverage beyond the currently completed language set
- explicit step-progress UI beyond section eyebrows
- richer triage-summary outputs beyond the current engine response, including support-needs summary and routing geography when those are implemented as canonical outputs
