[build]
	base = "apps/org-portal"
	command = "pnpm build:netlify"
	publish = "build"
	functions = ".netlify/functions-internal"

[build.environment]
	NODE_VERSION = "24.10.0"
	PUBLIC_SITE_URL = "https://primerpaso.org"
